#include <stdio.h>
#include <stdlib.h>
const int MAX = 5000;
int n, k;
typedef struct Node
{
  int data;
  struct Node *next;
} Node;

int main()
{
  Node *head, *tail;
  scanf("%d %d", &n, &k);

  return 0;
}
